<nav class="navbar navbar-expand-lg bg-primary" data-bs-theme="dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">
      <span class="badge bg-secondary p-3">CloneFlow</span>
    </a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarSupportedContent"
      aria-controls="navbarSupportedContent"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0 customNavbar">
        <% if(!locals.isAuthenticated) { %>
          <li class="nav-item">
            <a href="/signUp">Signup</a>
          </li>
          <li class="nav-item">
            <a href="/login">Login</a>
          </li>
          <% } else { %>
            <li class="nav-item">
              <a href="/questions">Question List</a>
            </li>
            <li class="nav-item">
              <a href="/askQuestion">Ask Question</a>
            </li>

            <% if(locals.isAdmin) { %>
            <li class="nav-item">
              <a href="/users">Users</a>
            </li>
          <% } %>
  
            <li class="nav-item"><a href="/about">About CloneFlow</a></li>
            <% } %>
      </ul>
      <% if(locals.isAuthenticated ) { %>
        <div class="d-flex align-items-center gap-3">
          <h5 class="mb-0 username">Welcome, <%= locals.user.name %></h5>
          <form id="clearForm" action="/logout" method="POST">
            <button>Logout</button>
          </form>
        </div>
        <% } %>
    </div>
  </div>
</nav>
